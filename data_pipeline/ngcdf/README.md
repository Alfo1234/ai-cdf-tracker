# NG-CDF Data Pipeline (Governance-Grade)

This pipeline is audit-first:

- Raw HTML snapshots are stored for verification.
- Parsed records follow a locked canonical JSON Schema.
- Governance intelligence is computed from freshness, completeness, and consistency.

Part 1 locks the contract and storage layout.
